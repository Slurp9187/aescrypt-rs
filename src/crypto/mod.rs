// src/core/crypto/mod.rs

//! Low-level crypto primitives (KDF).
//!
//! Sub-modules for primitives; see crate root for re-exports (e.g., `KdfBuilder`).
//! HMAC types are defined in `aliases.rs`.

pub mod kdf;
